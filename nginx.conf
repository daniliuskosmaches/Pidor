server {
    listen 80;

    root /usr/share/nginx/html;
    index index(1).html index.html;

    # 🚨 НОВЫЙ ВАРИАНТ: Указываем внешний адрес Вашего бэкенда
    # ЗАМЕНИТЕ ЭТУ СТРОКУ на фактический адрес Вашего сервера
    # Например, если Ваш бэкенд доступен по адресу https://api.myserver.com:8080

    location /api/booking {
    proxy_ssl_server_name on;

        # Используйте HTTPS, если Ваш внешний бэкенд использует его
        proxy_pass https://proxyserver1-np2f.onrender.com;


        # Пример: proxy_pass http://192.168.1.100:8080/;
        # Пример: proxy_pass https://proxy-server.ru:8080/;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # Если используете HTTPS:
        # proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ... (Остальная часть конфигурации Nginx без изменений)
    location / {
        try_files $uri $uri/ /index(1).html;
    }
}